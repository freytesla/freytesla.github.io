# 拉长石项链 · 物理版（重力 + 拖拽）实现指南

> 配套演示页：`necklace-physics.html`（仓库根目录），代码：`assets/js/necklace-physics.js`

## 一句话原理

**链子用网页端物理模拟**（64 个质点 + 距离约束，两端挂在展示架上，重力自然下垂），
**吊坠用你 Blender 做的模型**（导出 GLB 放进 `assets/models/` 即可自动替换）。
所以你在 Blender 里只需要认真做「蓝色拉长石吊坠」；**链子不用建模**——网页端直接渲染成细银绳
（蛇骨链质感，粗细在代码里调 `CHAIN_R`）。整条链的摆动、拖拽、回弹都是网页实时算的，
拖拽时整条链会像真项链一样被拉起来、松手甩动回弹。

```
Blender 建模蓝色拉长石吊坠 → 导出 .glb → assets/models/necklace.glb
                              ↓
     网页端(three.js)：Verlet 重力链 + 细银绳蛇骨链渲染 + 拖拽
```

## 1. 预览演示页

```powershell
cd C:\Users\Administrator\Desktop\frey.github.io
python -m http.server 8123
# 浏览器打开 http://localhost:8123/necklace-physics.html
```

- 按住链子或吊坠拖动 → 松手重力回弹、可甩动
- 右上角可调重力 g（0=无重力）/ 重置
- 页面打开时会有一段「坠落 + 回弹」的开场（重力演示）
- 当前默认吊坠是「蓝色拉长石（上短下长的风筝形/菱形）+ 扣环」的程序化兜底版，方便你先看效果

## 2. Blender 零基础怎么做「蓝色拉长石吊坠（风筝形）」

### 2.1 安装
去 https://www.blender.org/download/ 下载 LTS 版（4.x 均可），装完打开，界面默认中文（编辑 → 偏好设置 → 界面 → 语言）。

### 2.2 要不要从平面开始？—— 要

扁平的石头从**平面/圆面开始最省事**（比从球体开始好调）。推荐路线 A，备选 B。

#### 路线 A（推荐）：平面挤出成「风筝形」（上短下长的菱形）

1. `Shift + A` → 网格 → **平面（Plane）**
2. 右键 → **细分（Subdivide）** 1 次（切成 4×4 网格，方便拖出轮廓）
3. `Tab` 进编辑模式 → `A` 全选 → 按 `S` 再按 `Z` 输入 `1.3`，把平面拉成竖长矩形（约 2:3）
4. 拖出「风筝形」轮廓（用框选 `B` 选中一排顶点，按 `G` 拖动）——上端是短尖、中段最宽、底部是长长的尖：
   - **顶边**：选中最上面 1 排顶点，往中间收一点点（做成微拱）
   - **两侧（肩膀）**：选中第 2–3 排两侧的顶点，稍微往外拉（做出盾的两肩）
   - **底部**：选中底部中间 1–2 个顶点，往下拉（做出盾的尖底）
   - 边拖边从侧面/俯视看，直到像「盾牌」：上宽下尖
5. `A` 全选 → `E` 挤出 → 右键（取消移动，挤出一点点厚度）→ 再按 `S` 输入 `0.02`（压到很薄，约 2mm）
6. 右侧「添加修改器」→ **表面细分 Subdivision Surface** 2 级 → 右键 → **平滑着色**
   → 棱角变圆润，就是一颗打磨过的平底蛋面（cabochon）
7. **挂环（bail）**：`Shift + A` → 网格 → 圆环（Torus）→ 缩小到直径约 1cm → `R` 旋转 90° 竖起来 → 放在宝石顶部
8. 选中宝石 + 挂环 → `Ctrl + J` 合并 → 命名 `Pendant`

#### 路线 B（更简单，形状略圆）：立方体倒角

1. `Shift + A` → 立方体（Cube）→ `S` 再按 `Z` 输入 `0.1` 压扁
2. 添加「**倒角 Bevel**」修改器（数量 0.2，段数 3）→ 四角变圆 → 扁圆盾形
3. 编辑模式里拖动顶点微调成上宽下尖，再加挂环、合并命名

> 小贴士：两种路线做完，都可以再「编辑模式 → 选中外圈边 → 按 `E` 向内挤一圈」，做出宝石边缘的“台阶”，更像真拉长石吊坠的包边造型。

### 2.3 材质（拉长石虹彩）

**方案 A（推荐，最省事）**：网页端会自动给无贴图的模型套用「拉长石虹彩材质」（青灰 + 虹彩闪光）。
你在 Blender 里只要建好形状就行，材质颜色可以随便。

**方案 B（Blender 里做好材质再导出）**：
1. 选中宝石 → 材质属性 → 新建 → 用 Principled BSDF
2. 开启「薄膜」（Thin Film / IRI，在 Blender 4.x 的 Principled BSDF 里）
3. 薄膜厚度（Thin Film Thickness）设 300–600 nm，折射率 1.3–1.5 → 出现拉长石那种蓝绿闪光
4. 基础色用深青灰 `#3a4742`，粗糙度 0.1

glTF 导出会自动带上薄膜/虹彩（KHR_materials_iridescence），three.js 支持。

### 2.4 导出 GLB（关键设置）

1. 选中吊坠 → `Ctrl + A` → 全部变换（All Transforms）
2. 原点放到挂环顶部附近（右键 → 设置原点 → 原点到几何中心也行，网页端会自动归一化）
3. 文件 → 导出 → glTF 2.0：
   - 文件格式：**glTF Binary (.glb)**
   - 勾选：**仅选中项**（选吊坠）、**应用修改器**（Apply Modifiers）
   - 单位：米（默认即可）；方向默认 Y-Up 不用改
   - 贴图格式保持默认（不用压 Draco，网页端不装解码器）
4. 导出到 `assets/models/necklace.glb`（或 `pendant.glb`）

刷新演示页，右上角会显示「模型：Blender 模型」，吊坠就换成你的了。

## 3. 链子粗细 / 风格

链子默认是**细银绳蛇骨链**（`CHAIN_R = 0.017`，很细）。改粗细只需在 `assets/js/necklace-physics.js` 顶部：

```js
const CHAIN_R = 0.017;   // 链绳半径，越大越粗
const JOINT_SCALE = 1.0;  // 节点球半径 = 绳半径 → 丝滑无突起；想复刻蛇骨节理再调大（如 1.25）
```

`N`（质点数量）和 `SEG`（节长）决定链子密度：`N × SEG ≈ 链总长（米）`。

**材质**：链子用的是独立的高反光银材质 `chainSilver`（`roughness: 0.08` 低粗糙度 + `envMapIntensity: 2.8`
强环境反射，像抛光镜面）。想更哑光就把 `roughness` 调大（如 `0.3`），更亮就把 `envMapIntensity` 调大。
想链子更长就把 `SEG` 调大；更顺滑就调大 `N`。

## 4. 接到主站首页（index.html）

物理版目前是独立演示页。想替换首页 Hero 的静态旋转项链时，把 `home.js` 里的 `initNecklace()`
整体换成 `necklace-physics.js` 的 `initNecklacePhysics()`（导出该函数后，在 home.js 里 `import` 并调用），
并把 `<section id="hero">` 的舞台容器换成演示页的结构（`#stage` + `#stage-canvas`）。
两个实现互不影响，随时可以回退。


## 吊坠摆动参数（链子 / 扣子 / 石头双向约束）

新版不再让石头有一个独立的“假摆动力矩”，也不再在拖动时把最近链点瞬移过去。现在是一个统一的质量系统：

- **链子**：Verlet 质点与长度约束，有质量、会受重力和拖拽反作用力。
- **扣子**：一个有质量的可滑动接触点，被约束在链子的当前线段上，可以沿链子移动。
- **石头**：一个有质量的质心点，与扣子保持固定杆长，重力、拖拽力和链子张力都会参与求解。
- **视觉朝向**：由“扣子→石头”的真实方向决定，左右扭转只作为绕这条杆的额外转动。

拖动石头时，鼠标弹簧只作用于石头质心。石头通过刚性杆拉扣子，扣子再把力传给链子；链子移动时也会在同一物理子步中通过约束拉回扣子与石头，所以不再存在“链子先动、石头后追”的顺序。

首页参数在 `index.html` 的 `FREY_NECKLACE_OPTIONS.pendulum`：

```js
gravity: 9.8,
pendulum: {
  stoneMass: 3.6,          // 石头质量
  bailMass: 1.0,           // 扣子质量
  chainPointMass: 1.0,     // 单个链子质点质量

  dragSpring: 420,         // 鼠标拖动弹簧
  dragDamping: 32,          // 鼠标拖动阻尼
  maxDragAccel: 220,       // 拖拽加速度上限，防爆
  dragBailShare: 1.0,      // 手的力同步传给扣子的比例，避免只旋转不抬升
  pendantDrop: 0.08,       // 链子接触点相对扣环中心上移，使吊坠整体下垂
  stoneLift: 0.05,         // 石头在吊坠内部上移，缩短石头与扣环距离
  airDamping: 1.2,         // 石头空气阻尼
  bailDamping: 2.2,        // 扣子空气阻尼

  contactStiffness: 0.82,  // 扣子贴住链子的约束强度
  rodStiffness: 1.0,       // 扣子与石头之间杆长约束强度
  solverIterations: 10,    // 每个物理子步的联合求解次数

  twistSpring: 5.5,        // 石头绕杆左右扭转的回正力度
  twistDamping: 2.2,       // 左右扭转阻尼
  spin: 0.08,              // 平时主动左右扭转的力度
  motionFreq: 0.55,        // 主动扭转频率

  maxYawDeg: 45,           // 左右扭转软上限
  yawSoftness: 0.22,       // 超过软上限后的回正力度
  maxTiltDeg: 120,         // 总倾斜软上限
  tiltSoftness: 0.35       // 超过倾斜软上限后的回正力度
}
```

每个固定物理子步的计算顺序：

1. 链子质点积分：重力、阻尼、速度限制。
2. 扣子与石头积分：重力、空气阻尼、鼠标弹簧力。
3. 重复 `solverIterations` 次：
   - 链子相邻质点保持固定长度；
   - 扣子投影并约束到最近的链子线段；
   - 扣子与石头保持固定杆长；
   - 超过软倾角时，把石头缓缓拉回允许范围。
4. 根据当前位置变化回写速度，再根据扣子到石头的向量更新视觉朝向和左右扭转。

**拖动时不再冻结朝向**。现在只能抓石头本体；鼠标力、杆约束和链子反作用力会同时求解，所以石头会跟着手，但链子也会被石头直接拉动。

---


## 5. 调参速查（assets/js/necklace-physics.js 顶部）

| 参数 | 位置 | 作用 |
| --- | --- | --- |
| `N = 64` | init 内 | 链子质点数量（越多越细腻，也更重） |
| `SEG = 0.09` | init 内 | 每节长度（米） |
| `CHAIN_R = 0.017` | init 内 | 蛇骨链半径（很细） |
| `JOINT_SCALE = 1.0` | init 内 | 节点球倍率，1=丝滑无突起 |
| `pendantDrop = 0.12` | PEND | 链子接触点相对扣环中心上移，数值越大吊坠整体越下垂 |
| `rope.gravity` | 类内 | 重力加速度，首页为 9.8 |
| `damping = 0.995` | 类内 | 链点速度保留率 |
| `iterations = 8` | 类内 | 预落定和普通 step 的链约束迭代数 |
| `maxSpeed = 40` | 类内 | 单个链点速度上限 |
| `solverIterations = 10` | PEND | 运行时链子/扣子/石头的联合求解次数 |
浏览器控制台里可直接调：`window.__rope.gravity = 3`、`window.__rope.damping = 0.99`。

## 6. 常见问题

- **页面是黑屏/没反应？** 本页用了 ES Module，必须用 `python -m http.server` 或 GitHub Pages 打开，不能直接双击文件。
- **模型加载后位置不对/歪了？** 确认 Blender 里吊坠「挂环朝上」（Z 轴朝上），导出前 `Ctrl+A` 应用了变换。
- **链子太粗/太细？** 调 `CHAIN_R`。
- **想要链子更长/更短？** 调 `N` 和 `SEG`，二者乘积约等于链长（米）。
- **想要环环相扣的粗链？** 把 `CHAIN_R` 调大，`JOINT_SCALE` 保持 `1` 即可（丝滑粗绳）；纯链环风格需要换回 Torus 实例化渲染（旧代码见 git 历史）。
- **没有 WebGL 或用户开了「减少动态效果」？** 自动降级为静态项链图（与主站一致）。



